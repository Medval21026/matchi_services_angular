import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.matchi.service',
  appName: 'matchiService',
  webDir: 'dist/matchi_service_angular/browser'
};

export default config;
